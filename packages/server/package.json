{
  "name": "server",
  "scripts": {
    "config": "mkcert -install",
    "create:cert": "mkcert -key-file key.pem -cert-file cert.pem localhost",
    "start": "tsx src/server.mts"
  },
  "devDependencies": {
    "@simplewebauthn/types": "^10.0.0",
    "@tsconfig/node20": "^20.1.4",
    "@types/node": "^22.5.4",
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "^10.0.1"
  }
}
